scenes.SCENENAME = () => {

    fadeIn(500, true);

    return {
        // Pre-render function
        preRender(ctx, delta) {

        },
        // Controls
        controls: [
            
        ],
        name: "ex"
    }
}